import { GetterTree } from 'vuex';
import { StateInterface } from '../index';
import { ThirdYear2ndSemStateInterface } from './state';

const getters: GetterTree<ThirdYear2ndSemStateInterface, StateInterface> = {
  someAction(/* context */) {
    // your code
  },
};

export default getters;
